export const totalRegistrationsItems = (state) => {
    return state.totalRegistrationsItems
}

export const totalRegistrationPending = (state) => {
    return state.totalRegistrationPending
}

export const totalRegistrationsAndActivationsItems = (state) => {
    return state.totalRegistrationsAndActivationsItems
}

export const totalRegistrationsAndActivationsPending = (state) => {
    return state.totalRegistrationsAndActivationsPending
}
